# アプリ
API_PORT=5000
WEB_PORT=3000

# Firebase（本番は Firebase Console のプロジェクト ID、ローカルはエミュレータ用）
FIREBASE_PROJECT_ID=demo-hakkason

# Firestore コレクション（チームで統一）
# materials: { title, description? }
# progressRecords: { materialId, materialTitle, progressPercent, note, recordedAt, createdAt }
